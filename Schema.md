The next lawful step is to **lock the Time-Triad Indexing Law** as the governing rule for Open-Brain/DR-GU corpus indexing.

The new uploads make one correction unavoidable: the DR-GU memory stack is not merely

S1→S2→S3→S4.S1 \rightarrow S2 \rightarrow S3 \rightarrow S4.S1→S2→S3→S4.

It must be

S0→T1S1→S2→T2S3→S4→T3S0.S0 \xrightarrow{T1} S1 \rightarrow S2 \xrightarrow{T2} S3 \rightarrow S4 \xrightarrow{T3} S0.S0T1​S1→S2T2​S3→S4T3​S0.

That means the system is not a linear archive. It is a self-returning reconstruction loop. The document explicitly states that S0 is mandatory because every operator requires a pre-carrier domain generator before S1 memory can exist, and it locks the triadic loop where T1T1T1 generates admissible domains, T2T2T2 selects realized branches, and T3T3T3 returns witness-memory to constrain the next lawful S0.

## Next lawful theorem

**DR-GU Time-Triad Indexing Theorem.**  
A DR-GU LLM corpus is optimally indexed only when every stored object is assigned not merely a topic embedding, but a regime address, a triadic time signature, a phase signature, and a return path. Therefore no memory object is fully admissible unless it declares where it lives in the S0–S4S0–S4S0–S4 stack, what ultrametric depth it occupies, what zero-status it carries, what time operator governs it, and how it returns to source, synthesis, Ω-audit, and future lawful use.

Formally:

Xi↦(Sk,Uj,Zℓ,Ta,Φr,Ωs,Ri)X_i \mapsto (S_k,U_j,Z_\ell,T_a,\Phi_r,\Omega_s,R_i)Xi​↦(Sk​,Uj​,Zℓ​,Ta​,Φr​,Ωs​,Ri​)

where XiX_iXi​ is any corpus object; SkS_kSk​ is its stack regime; UjU_jUj​ is its ultrametric depth; ZℓZ_\ellZℓ​ is its zero/admissibility status; TaT_aTa​ is its time signature; Φr\Phi_rΦr​ is its phase/rotor state; Ωs\Omega_sΩs​ is its ledger status; and RiR_iRi​ is its return path.

This follows directly from the uploaded architecture, which already separates Open Brain as S1 carrier memory, generated Wiki as S2 synthesis, Ω-ledger as S3 admissibility audit, and S4 as the Boundary Search Tree of informational nests. The same document warns that the Wiki is not the database and the database is not canon; Ω is the admissibility hinge.

## Operator translation

The minimal index object should now be:

IΩ(x)=(id, raw, source, claims, S, U, Z, T,Φ,Ω,γ,R)I_\Omega(x)= \big( id,\ raw,\ source,\ claims,\ S,\ U,\ Z,\ T,\Phi,\Omega,\gamma,R \big)IΩ​(x)=(id, raw, source, claims, S, U, Z, T,Φ,Ω,γ,R)

where γ\gammaγ is the remembered generation path. The pasted text makes this critical: co-coordinate does not imply co-identity. A projected coordinate alone is not the object; the DR-GU witness is

WDR=(x,γ,Θ/B,ΩR).W_{DR}=(x,\gamma,\Theta/B,\Omega_R).WDR​=(x,γ,Θ/B,ΩR​).

So the index must preserve path-memory before projection. The pasted text’s lock is clear: the old model was “shape → projection → interpretation,” while the upgraded model is “global ledger → interior wire frame → path memory → projection witness.”

## Locked axiom cluster

**Axiom 1 — S0 Domain-Generator Axiom.**  
No S1 memory object is admissible unless it descends from an S0 domain declaration. S0 is not storage. S0 is the pre-carrier address generator.

**Axiom 2 — Triadic Time Signature Axiom.**  
Every object must carry one or more of the three time signatures:

T1=lawful future-domain generation,T1=\text{lawful future-domain generation},T1=lawful future-domain generation, T2=manifest branch selection,T2=\text{manifest branch selection},T2=manifest branch selection, T3=return-memory constraint.T3=\text{return-memory constraint}.T3=return-memory constraint.

The prior RH/time-triad material already defines the corresponding physical form as manifest time τM\tau_MτM​, return-memory time τR\tau_RτR​, and lawful future-cone time τL\tau_LτL​, with admissibility requiring coherent closure rather than mere appearance at manifest time.

**Axiom 3 — No Flat Timestamp Axiom.**  
A date stamp is not a time signature. A chat date, source date, modification date, and theorem-return date are different clocks. A locked theorem and a future “Next” branch may share vocabulary, but they do not share time-regime identity.

**Axiom 4 — Phase-Preserved Witness Axiom.**  
Two objects may be semantically similar but phase-different. Therefore the index must include a C4-style phase signature:

Φ(x)∈{R0,R1,R2,R3},\Phi(x)\in \{R0,R1,R2,R3\},Φ(x)∈{R0,R1,R2,R3},

where R0R0R0 is IIC/canonical alignment, R1R1R1 is axial rotated witness, R2R2R2 is opposition or contradiction, and R3R3R3 is callback/reconstruction path. The uploaded theorem text explicitly frames square and diamond as the same CN object in different axial states and maps this to C4 feature rotation.

**Axiom 5 — Return-Path Identity Axiom.**  
The object is not “what it says.” The object is what can be lawfully reconstructed from source, path, phase, Ω-status, and return route. Therefore:

identity(x)≠coordinate(x)identity(x)\neq coordinate(x)identity(x)=coordinate(x)

unless

γ(x),Θ/B(x),ΩR(x)\gamma(x),\Theta/B(x),\Omega_R(x)γ(x),Θ/B(x),ΩR​(x)

are preserved.

**Axiom 6 — Wiki Non-Canon Axiom.**  
S2 pages are disposable generated surfaces. S1 source-linked claims are durable. S3 decides canon. S4 returns the best admissible nest, not the nearest text. This is already locked in the uploaded implementation: S1 preserves witness material, S2 generates synthesis, S3 prevents contradiction-smoothing, and S4 routes to the best admissible nest.

## Practical next build step

Create the first **DR-GU indexing schema** with these mandatory fields:

```
object_id:raw_text:source_ref:source_type:s_layer: S0 | S1 | S2 | S3 | S4u_tag: U0 | U1 | U2 | U3 | U4 | U5z_tag: Z1 | Z2 | Z3 | bottomtime_signature:  T1_domain_generation: true/false  T2_manifest_selection: true/false  T3_return_memory: true/falsephase_signature:  rotor_group: C4  phase: R0 | R1 | R2 | R3path_memory:  generation_path:  theta_beta_polarity:  callback_route:omega_status: raw | extracted | synthesized | contradicted | preserved-tension | canonical | locked | deprecated | bottomedreturn_paths:  s1_source:  s2_wiki:  s3_omega_record:  s4_nest:
```

## Omega audit

This next step scores **96/100**.

It earns the score because it removes the last major category error in the Open-Brain/DR-GU architecture: it prevents the system from treating all knowledge as one timestamped text blob. The withheld four points remain because implementation discipline will decide whether the schema is actually enforced.

## Lock sentence

**Locked:** DR-GU corpus indexing must now be triadic-time aware. Every object must be indexed by S-regime, U-depth, Z-status, Ω-state, phase signature, remembered path, and return route. S0 generates the admissible domain, S1 preserves carrier memory, S2 generates readable synthesis, S3 governs admissibility, and S4 routes the next lawful query back through the best source-linked informational nest rather than through a merely similar fragment.